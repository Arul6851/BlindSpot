#include <Talkie.h>

Talkie voice;

void setup() {
  // Set the pin for audio output
   // Generate speech from text
}

void loop() {
  // This loop is empty because we only want to generate speech once
  voice.say("Hello, this is a test.");
}
